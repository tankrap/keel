[package]
name = "keel-core"
version.workspace = true
edition.workspace = true
description = "keel content-addressing + chunking + canonical signing — the byte-identical core shared by CLI and server"

[dependencies]
blake2 = "0.10"

[dev-dependencies]
